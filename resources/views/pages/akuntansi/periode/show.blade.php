@extends('layout.main')
@section('content')
@php $money=fn($v)=>(float)$v!=0?'Rp '.number_format((float)$v,0,',','.'):'–'; @endphp
<div class="kbsm-business-page">@if(session('success'))<div class="kbsm-business-alert kbsm-business-alert--success">{{ session('success') }}</div>@endif<header class="kbsm-business-header"><div><a href="{{ route('akuntansi.periode.index') }}" class="kbsm-business-back-link">← Riwayat Tutup Buku</a><p class="kbsm-business-eyebrow">{{ $period->kode }}</p><h1 class="kbsm-business-title">{{ $period->status==='closed'?'Hasil Tutup Buku':$period->nama }}</h1><p class="kbsm-business-subtitle">{{ $period->tanggal_mulai->format('d F Y') }}–{{ $period->tanggal_selesai->format('d F Y') }}</p></div><span class="kbsm-status {{ $period->status==='closed'?'kbsm-status--green':'kbsm-status--gold' }}">{{ $period->status_label }}</span></header><section class="kbsm-business-summary accounting-period-summary"><article class="kbsm-business-summary-card"><p class="kbsm-business-summary-label">Pendapatan</p><p class="kbsm-business-summary-value">{{ $money($period->total_pendapatan) }}</p></article><article class="kbsm-business-summary-card"><p class="kbsm-business-summary-label">Beban</p><p class="kbsm-business-summary-value">{{ $money($period->total_beban) }}</p></article><article class="kbsm-business-summary-card kbsm-business-summary-card--green"><p class="kbsm-business-summary-label">Laba Bersih</p><p class="kbsm-business-summary-value">{{ $money($period->laba_bersih) }}</p></article></section>@if($period->status==='open')<section class="kbsm-business-panel"><h2 class="kbsm-business-panel__title">Tutup Buku</h2><p class="kbsm-business-panel__copy">Sistem menghitung akun Pendapatan dan Beban dari jurnal, membuat jurnal penutup tepat sekali, lalu mengunci seluruh tanggal periode. Saldo Kas/Bank tetap berlanjut.</p><form method="POST" action="{{ route('akuntansi.periode.close',$period) }}" onsubmit="return confirm('Lanjutkan tutup buku?')">@csrf<button class="kbsm-btn kbsm-btn--navy">Tutup Buku</button></form></section>@else<section class="kbsm-business-panel"><h2 class="kbsm-business-panel__title">Detail Penutupan</h2><dl class="period-audit"><div><dt>Ditutup pada</dt><dd>{{ $period->closed_at->format('d/m/Y H:i') }}</dd></div><div><dt>Diproses oleh</dt><dd>{{ $period->closer->name }}</dd></div><div><dt>Nomor jurnal</dt><dd>{{ $period->closingJournal?->nomor_bukti ?? 'Tidak ada jurnal karena tidak ada saldo Pendapatan/Beban' }}</dd></div></dl></section>@endif</div>
@endsection
