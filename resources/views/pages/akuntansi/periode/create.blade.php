@extends('layout.main')
@section('content')
<div class="kbsm-business-page">
    <header class="kbsm-business-header"><div><a href="{{ route('akuntansi.periode.index') }}" class="kbsm-business-back-link">← Riwayat Tutup Buku</a><p class="kbsm-business-eyebrow">Akuntansi</p><h1 class="kbsm-business-title">Buka Periode</h1><p class="kbsm-business-subtitle">Periode wajib tepat satu tahun dan tidak boleh bertumpang tindih.</p></div></header>
    @if($errors->any())<div class="kbsm-business-alert kbsm-business-alert--danger"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
    <section class="kbsm-business-panel"><form method="POST" action="{{ route('akuntansi.periode.store') }}" class="kbsm-form-grid">@csrf<div class="kbsm-field"><label>Kode Periode</label><input name="kode" value="{{ old('kode', 'FY-' . today()->year) }}" required></div><div class="kbsm-field"><label>Nama Periode</label><input name="nama" value="{{ old('nama', 'Tahun Buku ' . today()->year) }}" required></div><div class="kbsm-field"><label>Tanggal Mulai</label><input type="date" name="tanggal_mulai" value="{{ old('tanggal_mulai', today()->startOfYear()->toDateString()) }}" required></div><div class="kbsm-field"><label>Tanggal Selesai</label><input type="date" name="tanggal_selesai" value="{{ old('tanggal_selesai', today()->endOfYear()->toDateString()) }}" required></div><div class="kbsm-field kbsm-field--wide"><a href="{{ route('akuntansi.periode.index') }}" class="kbsm-btn kbsm-btn--outline-slate">Batal</a> <button class="kbsm-btn kbsm-btn--navy">Buka Periode</button></div></form></section>
</div>
@endsection
