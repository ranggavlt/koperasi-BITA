@extends('layout.main')
@section('content')
<div class="kbsm-business-page"><header class="kbsm-business-header"><div><a href="{{ route('akuntansi.periode.index') }}" class="kbsm-business-back-link">← Riwayat Tutup Buku</a><p class="kbsm-business-eyebrow">Akuntansi</p><h1 class="kbsm-business-title">Mulai Periode</h1><p class="kbsm-business-subtitle">Satu periode berjalan harus ditutup sebelum periode berikutnya dibuka.</p></div></header>@if($errors->any())<div class="kbsm-business-alert kbsm-business-alert--danger"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<section class="kbsm-business-panel"><form method="POST" action="{{ route('akuntansi.periode.store') }}" class="accounting-period-form">@csrf<div class="kbsm-business-field"><label class="kbsm-business-label">Kode Periode</label><input class="kbsm-business-control" name="kode" value="{{ old('kode','FY-'.today()->year) }}" required></div><div class="kbsm-business-field"><label class="kbsm-business-label">Nama Periode</label><input class="kbsm-business-control" name="nama" value="{{ old('nama','Tahun Buku '.today()->year) }}" required></div><div class="kbsm-business-field"><label class="kbsm-business-label">Tanggal Mulai</label><input class="kbsm-business-control" type="date" name="tanggal_mulai" value="{{ old('tanggal_mulai',today()->startOfYear()->toDateString()) }}" required></div><div class="kbsm-business-field"><label class="kbsm-business-label">Tanggal Selesai</label><input class="kbsm-business-control" type="date" name="tanggal_selesai" value="{{ old('tanggal_selesai',today()->endOfYear()->toDateString()) }}" required></div><div class="kbsm-business-actions"><a href="{{ route('akuntansi.periode.index') }}" class="kbsm-btn kbsm-btn--outline-slate">Batal</a><button class="kbsm-btn kbsm-btn--navy">Mulai Periode</button></div></form></section></div>
@endsection
